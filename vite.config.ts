import { defineConfig } from 'vite';

export default defineConfig({
  base: '/SnipeStudy_AI-COMPANION/',
  // Other configuration options can go here
  // For example, if you're using React, you might have:
  // plugins: [react()], 
});
